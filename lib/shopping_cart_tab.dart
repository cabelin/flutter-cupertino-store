import 'package:flutter/cupertino.dart';

class ShoppingCartTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ShoppingCartTab'),
    );
  }
}
